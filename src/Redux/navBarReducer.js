let initialState = {
    friends: [
        { name: 'Alex' },
        { name: 'Liza' },
        { name: 'Leha' }
    ]
}

export const navBarReducer = (state = initialState, action) => {

    
    return state;
}