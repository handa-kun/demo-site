import styles from './Login.module.css';

const Login = (props) => {
    return <div className={styles.form}>
        <h1>LOGIN</h1>
        <p>Your Email</p>
        <p>Your Password</p>
    </div>
}

export default Login